import type { Livery } from "./theme";

/**
 * The Pipeline Tracker wears Senna. Fixed at build time, not a preference —
 * see lib/theme.ts for why livery is per app and only polarity is shared.
 */
export const LIVERY: Livery = "senna";
