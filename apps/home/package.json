{
  "name": "home",
  "version": "0.1.0",
  "engines": {
    "node": "24.x"
  },
  "private": true,
  "scripts": {
    "generate": "node scripts/collect-declared.mjs && node scripts/collect-drift.mjs",
    "predev": "npm run generate",
    "dev": "next dev",
    "prebuild": "npm run generate",
    "build": "next build",
    "start": "next start",
    "pretest": "npm run generate",
    "test": "vitest run",
    "declared": "node scripts/collect-declared.mjs",
    "drift": "node scripts/collect-drift.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "next": "^14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.9"
  }
}
