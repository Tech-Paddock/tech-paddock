{
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"preview\" ]; then r=\"$(git rev-parse --show-toplevel 2>/dev/null)\" && [ -n \"$r\" ] && grep -qxF \"home\" \"$r/.claude/ON-TRACK\" 2>/dev/null && exit 1; exit 0; fi; cd \"$(git rev-parse --show-toplevel)\" || exit 1; git diff --quiet HEAD^ HEAD -- apps/home packages .claude || exit 1; exit 0"
}
