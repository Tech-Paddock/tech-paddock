import type { Livery } from "./theme";

/**
 * The Resume Formatter wears MP4/4. Fixed at build time, not a preference —
 * see lib/theme.ts for why livery is per app and only polarity is shared.
 */
export const LIVERY: Livery = "mp44";
