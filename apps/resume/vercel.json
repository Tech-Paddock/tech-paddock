{
  "framework": "nextjs",
  "ignoreCommand": "R=$(git rev-parse --show-toplevel 2>/dev/null); if [ \"$VERCEL_ENV\" = preview ]; then grep -qxF resume \"$R/.claude/ON-TRACK\" && exit 1; exit 0; fi; cd \"$R\" || exit 1; git diff --quiet HEAD^ HEAD -- apps/resume packages || exit 1; exit 0"
}
